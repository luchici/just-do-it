import styles from "./Calendar.module.scss";

function Calendar(props) {
  return (
    <div className={styles.calendar}>
      <p className={styles.text}>Google Caledar API</p>
    </div>
  );
}

export default Calendar;
